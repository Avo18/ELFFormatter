#pragma once
//template<typename Type> class ELFBufferBytes
//{
//private:
//	const char* buffer;
//	size_t size;
//	size_t offset;
//
//public:
//	ELFBufferBytes(const char* buffer, size_t size);
//	void read(Type& value);
//	void read(Type(&chars)[count]);
//};